# 节日福利礼物发放与库存管理系统 — 开发前工作文档

> 适用于 vibecoding 起项目，作为 PRD + 工程参考文档使用。  
> 目标读者：PM (自查)、AI 编程助手 (上下文)、未来的协作者。

---

## 0. 文档定位

这不是一份"功能清单",而是一份**让你能在 vibecoding 时持续校准方向的产品决策文档**。三个用法：

1. 开始 vibecoding 时整段贴给 AI 当系统提示
2. 开发到某个模块时,把对应章节单独贴出来做局部对齐
3. 上线/汇报时,作为产品决策的复盘依据

---

## 1. 问题定义 (Problem Statement)

**背景**:某公司 3000 人规模,集中在同城多个办公楼,行政团队负责每个节点的福利礼物发放(春节、端午、中秋、女神节、儿童节、生日、司庆等)。

**当前痛点假设(开发前需验证)**:

| 角色 | 痛点 |
|---|---|
| 行政同学 | Excel + 群发通知 + 微信接龙;名单更新滞后;库存靠人脑;离职/调岗易出错;高峰期一个节日要花 3-5 天 |
| 普通员工 | 不知道节点、不知道在哪领、不知道能不能选;领取要排队;错过窗口期没法补领 |
| 分仓行政 | 不知道今天来领的人是不是真的有资格;领没领过靠纸质勾选;盘点对不上 |
| 财务/采购 | 拿不到准确的领取数据做对账和下次采购预测 |

**为什么现在做**:节日是高频且强情绪触点,做不好是负反馈;3000 人规模 Excel 已经管不住;同城多楼宇意味着**分布式发放**,这是必须系统化的核心拐点。

---

## 2. 北极星指标与衡量框架

**北极星指标 (North Star)**:  
**福利发放净推荐分 NPS × 按时完成率**

> 不能只看"发完了",要看"舒服地发完了"。

**辅助指标 (按重要性排序)**:

1. **按时领取率** = 截止前完成领取的员工 / 应发员工 (目标 ≥ 95%)
2. **库存差异率** = |系统库存 - 实盘| / 总库存 (目标 ≤ 1%)
3. **行政人均处理时长** = 行政总投入工时 / 批次发放员工数 (目标比当前 ↓ 60%)
4. **员工投诉/工单数** = 每批次产生的异常处理数 (目标 ≤ 0.5%)
5. **员工满意度 CSAT** = 领取后评分平均分 (目标 ≥ 4.5/5)

---

## 3. 用户画像 (Personas)

### P1 — 行政统筹小 A (主用户,管理端)
- **职责**:全公司福利策划、采购对接、发放统筹
- **场景频率**:每年 6-8 次大批次 + 滚动生日礼物
- **核心诉求**:批量、准确、可追溯、能甩手
- **典型抱怨**:"上次中秋我比员工先吐(月饼)"、"每次都怕漏发新员工"

### P2 — 楼宇分仓行政小 C (执行端)
- **职责**:在自己楼宇仓库做实际收发
- **核心诉求**:核身份快、不出错、能盘点
- **设备约束**:手机 + 偶尔有 PC,工位不固定

### P3 — 普通员工小 B (消费端,占用户量 99%)
- **职责**:领礼物 (不是工作的一部分)
- **核心诉求**:**少打扰、快、能选**
- **关键洞察**:他们不会专门打开你的系统,**必须通过 IM 通知触达**

### P4 — HR / 财务 (辅助)
- **诉求**:数据导出、对账、预算汇总
- **MVP 阶段**:能导 Excel 即可

---

## 4. JTBD (Jobs to Be Done)

> JTBD 是测试功能是否真的需要的尺子。任何不能映射到下面某个 Job 的功能,默认砍掉。

| 角色 | When (情境) | I want to (动作) | So that (目的) |
|---|---|---|---|
| 行政 A | 当节日临近 | 批量、按规则地把礼物精准送到员工手上 | 我可以专注更高价值工作而不用反复对名单 |
| 行政 A | 当我需要补充库存或盘点 | 实时知道每个楼宇还剩多少、领了多少 | 不会超发或断货 |
| 行政 C | 当员工到分仓来领 | 快速核验身份 + 自动扣库存 | 我不用记账本、不用怕被冒领 |
| 员工 B | 当公司发福利时 | 在手机上 1 分钟内完成选择和预约领取 | 我能感受到关怀但不被打扰工作 |
| 员工 B | 当我错过领取时间 | 知道还能不能补领、怎么补 | 我不会有"被遗忘"的负面感受 |

---

## 5. 核心场景矩阵

**这是切 MVP 范围的关键依据**。把所有可能的发放场景列出来,按"频次 × 复杂度"打分。

| 场景代号 | 描述 | 频次/年 | 复杂度 | MVP? |
|---|---|---|---|---|
| S1 | **统一礼物全员发** (中秋月饼,人手一份) | 2-3 次 | 低 | ✅ Must |
| S2 | **多 SKU 自选** (端午粽子礼盒 A/B/C 选一) | 2-3 次 | 中 | ✅ Must |
| S3 | **差异化人群** (女神节限女员工、儿童节限有娃员工) | 2 次 | 中 | ✅ Must |
| S4 | **滚动型生日礼物** (每月生日员工自动触发) | 月度 | 中 | 🟡 Should |
| S5 | **司龄分级** (周年庆按 1/3/5/10 年发不同礼物) | 1 次 | 高 | 🟡 Should |
| S6 | **配送上门** (孕妇、出差中员工) | 偶发 | 高 | ⚪ Could |
| S7 | **跨楼宇调拨** (A 楼库存不够从 B 楼调) | 偶发 | 中 | ⚪ Could |
| S8 | **代领/转赠** | 偶发 | 中 | ⚪ Could |

**MVP 锁定 S1 + S2 + S3**。这三个组合覆盖了 80% 的发放场景,且共享同一套数据模型。

---

## 6. 用户旅程地图 (核心两条主线)

### 旅程 A:行政端"一次发放"全周期

```
[策划]              [配置]              [触达]              [发放]              [收尾]
  │                  │                  │                  │                  │
创建批次   →   设置 SKU、库存、规则、    →   推送通知    →   员工陆续领取    →   关单 + 报表
            分仓、领取时间窗、人群       (IM + 兜底)      (扫码核销)        (剩余处理)
            └─ 关键决策点:               └─ 关键决策点:    └─ 关键决策点:
              人群规则引擎是否好用         催领节奏           盘点闭环
```

**关键体验时刻 (Moment of Truth)**:
- 配置阶段:**规则引擎不能比 Excel 还难用** (否则行政会回去用 Excel)
- 收尾阶段:**剩余库存自动给出处理建议** (回库/邮寄/废弃)

### 旅程 B:员工端"一次领取"全周期

```
[感知]              [决策]              [行动]              [反馈]
  │                  │                  │                  │
收到 IM 通知   →   点开 H5/小程序   →   到分仓出示码 → 领走 + 评分
              查看可选项目          (可选:预约时段)
              选择 SKU
              选择领取楼宇
              生成领取码
                  │
              └─ 关键体验:**3 步内完成,不需要登录账号**
              (用企业 SSO 静默授权)
```

**反摩擦原则**:员工每多操作一步,流失率 +15%。MVP 阶段坚持「**一条链接进入 → 看到我能领什么 → 一键确认 → 拿码走人**」。

---

## 7. 功能架构与 MVP 范围

### 7.1 模块树

```
节日福利系统
├── 管理后台 (Admin)
│   ├── 批次/活动管理        [Must]
│   ├── SKU/礼物管理         [Must]
│   ├── 库存与分仓管理       [Must]
│   ├── 人群规则引擎         [Must]  ← 难点
│   ├── 名单导入/HR同步      [Must]
│   ├── 通知中心             [Must]
│   ├── 数据看板             [Must]
│   ├── 异常工单             [Should]
│   └── 预算/采购预测        [Could]
│
├── 员工端 (Employee, H5/小程序)
│   ├── 我的福利            [Must]
│   ├── 礼物选择            [Must]
│   ├── 领取码生成          [Must]
│   ├── 领取记录            [Must]
│   ├── 评价反馈            [Should]
│   └── 代领授权            [Could]
│
└── 分仓端 (Warehouse, 移动优先)
    ├── 扫码核销             [Must]
    ├── 我的待领清单         [Must]
    ├── 实时库存             [Must]
    ├── 盘点                 [Should]
    └── 跨仓调拨申请         [Could]
```

### 7.2 MoSCoW 优先级 (vibecoding MVP 锁定 Must)

**Must (MVP 必做,2-3 天 vibecoding 量级)**
1. 批次创建 (选择节日类型、时间窗、关联 SKU)
2. SKU 管理 (名称、图、描述、价值)
3. 分仓库存管理 (每个 SKU 在每个楼宇的库存)
4. 简单人群规则 (全员 / 按部门 / 按性别 / 按司龄,布尔组合)
5. 员工名单 Excel 导入
6. 员工 H5 端:查看、选择、生成领取码 (二维码)
7. 分仓端:扫码核销 (扫员工码 → 系统自动扣库存 + 标记已领)
8. 实时数据看板 (按批次/楼宇/SKU 多维度)

**Should (V1.1)**
- 飞书 / 邮件通知推送
- 自动催领 (截止前 24h/2h)
- 异常工单 (员工申诉、缺货登记)
- 生日礼物滚动批次

**Could (V2)**
- 配送上门
- 跨楼宇调拨
- 代领授权
- 满意度收集
- 预算管理

**Won't (本系统不做)**
- 采购流程 / 供应商管理 → 用现有 ERP/采购系统
- 财务对账 → 导出 Excel 给财务
- 礼物商城/积分体系 → 这是另一个产品

---

## 8. 数据模型 (Vibecoding 时直接用)

```typescript
// 员工 (HR 同步,只读)
Employee {
  id: string
  employee_no: string         // 工号
  name: string
  gender: 'M' | 'F' | 'Other'
  department: string
  building: string            // 默认所在楼宇
  hire_date: date
  birth_date: date
  has_children: boolean       // 用于儿童节
  status: 'active' | 'leave' | 'resigned'
}

// 礼物 SKU
Gift {
  id: string
  name: string
  description: string
  image_url: string
  value: number               // 估值,做预算用
  category: string            // 食品/日用/数码...
  allergen_tags: string[]     // 过敏原标签(如"含坚果"),食品类必填
}

// 分仓 (楼宇)
Warehouse {
  id: string
  building_name: string       // 如"科技园 A 座"
  address: string
  manager_employee_id: string
}

// 库存
Inventory {
  id: string
  warehouse_id: string
  gift_id: string
  qty_total: number           // 总数
  qty_reserved: number        // 已被预约/锁定
  qty_claimed: number         // 已发放
  qty_available: number       // = total - reserved - claimed (派生字段)
}

// 批次/活动
Campaign {
  id: string
  name: string                // "2026 中秋福利"
  holiday_type: string        // mid_autumn / spring_festival / ...
  start_at: datetime          // 领取开始时间
  end_at: datetime            // 领取截止时间
  selection_mode: 'fixed' | 'choose_one'  // 固定礼物 vs 多选一
  eligibility_rule: JSON      // 见下方规则引擎
  status: 'draft' | 'published' | 'active' | 'closed'
  created_by: string
}

// 批次包含的礼物 (多对多)
CampaignGift {
  campaign_id: string
  gift_id: string
  qty_per_employee: number    // 通常为 1
}

// 资格名单 (生成自规则引擎,可手动调整)
Eligibility {
  campaign_id: string
  employee_id: string
  status: 'eligible' | 'excluded' | 'claimed' | 'expired'
}

// 领取记录
Claim {
  id: string
  campaign_id: string
  employee_id: string
  gift_id: string             // 员工选择的 SKU
  warehouse_id: string        // 员工选择的领取楼宇
  claim_code: string          // 6位数字 + 二维码
  status: 'reserved' | 'claimed' | 'cancelled' | 'expired'
  reserved_at: datetime
  claimed_at: datetime?
  claimed_by: string?         // 分仓行政 id (核销人)
}
```

### 8.1 人群规则引擎 (DSL 示意)

为了让行政不用写代码,但又能组合规则,设计成可视化的布尔规则树:

```json
{
  "type": "AND",
  "rules": [
    { "field": "status", "op": "=", "value": "active" },
    {
      "type": "OR",
      "rules": [
        { "field": "gender", "op": "=", "value": "F" },
        { "field": "has_children", "op": "=", "value": true }
      ]
    },
    { "field": "hire_date", "op": "<=", "value": "2026-01-01" }
  ]
}
```

UI 上做成嵌套块状结构,行政点击「+ 条件」「+ 分组」逐层添加。这是后台体验的关键差异点。

---

## 9. 核心流程时序 (4 个最关键流程)

### 9.1 员工领取流程 (最核心)

```
员工         H5         后端          分仓行政        库存
 │           │           │               │           │
 ├─收 IM 通知 │           │               │           │
 ├─点链接   ──>│           │               │           │
 │           ├─SSO 静默授权 >│             │           │
 │           │<─返回可领批次─│             │           │
 │<──展示选项─│           │               │           │
 ├─选 SKU+楼宇>│           │               │           │
 │           ├─创建 Claim ──>│             │           │
 │           │           ├─锁库存 ──────────────────>│
 │<──显示二维码 ←─返回 claim_code           │           │
 │                                       │           │
 │ ─── (走到分仓出示二维码) ──>            │           │
 │                                       ├─扫码      │
 │                                       ├─请求核销 ─>后端验证
 │                                       │<─返回员工信息+礼物
 │                                       ├─发放实物   │
 │                                       ├─确认核销 ─>后端
 │                                       │           ├─扣库存 (reserved→claimed)
 │<──IM 通知"领取成功"                     │           │
```

### 9.2 库存状态机

```
[创建采购]──入库──> available
                      │
                      ├──员工预约──> reserved ──核销──> claimed
                      │                  │
                      │                  └──超时/取消──> available
                      │
                      └──盘亏/损耗──> written_off
```

### 9.3 批次生命周期状态机

```
draft ──发布──> published ──到时间──> active ──截止──> closed
   ↑                │                    │
   └───── 编辑 ─────┘                    └──> 自动生成结算报告
```

### 9.4 异常处理流程

| 异常 | 处理方式 |
|---|---|
| 员工预约但未领 | 截止时间后 N 小时自动取消,库存回 available |
| 库存不足无法预约 | 显示"该楼宇缺货",建议切换其他楼宇或加入候补 |
| 离职员工已预约 | HR 同步后自动取消预约,通知行政 |
| 员工反映领错/损坏 | 走异常工单,行政手动审批补发 (V1.1) |
| 楼宇分仓盘点不符 | 行政手动调整库存,留审计日志 |

---

## 10. 关键产品决策与权衡

> 这部分是真正的 PM 价值所在。每个决策都有 trade-off,写下来防止 vibecoding 时漂移。

### 决策 1:员工端用 H5 还是小程序?
**选 H5**。理由:
- 公司有 SSO,H5 静默授权最快
- 无需安装,IM 一点即开
- 维护成本低
- 代价:扫码核销稍弱,但分仓端用专门 App 弥补即可

### 决策 2:核销码用静态 vs 动态?
**选静态 6 位数字 + 二维码,绑定 claim_id**。理由:
- 一码一用,核销后失效
- 不联网也能扫(分仓 App 缓存有效码列表)
- 比动态码简单,vibecoding 阶段够用

### 决策 3:库存按"批次锁定"还是"全局共享"?
**选全局共享 + 软锁定 (reserved)**。理由:
- 同一 SKU 可能在多个批次复用(下次中秋还能用)
- 锁定到批次会导致库存利用率低
- 软锁定通过 reserved_qty 保证不会超发

### 决策 4:规则引擎做多复杂?
**MVP 只支持单层 AND/OR + 5 个字段** (部门、性别、楼宇、司龄、是否有娃)。理由:
- 覆盖 80% 场景
- 嵌套规则的 UI 是无底洞
- 复杂规则可以「上传 Excel 名单」兜底

### 决策 5:要不要做"我的同事帮我领"?
**MVP 不做**。理由:
- 涉及授权链、身份核验,复杂度爆炸
- 真实场景比例低 (估计 < 3%)
- 兜底方案:行政可手动改预约人

---

## 11. 异常 & 风险清单 (Edge Cases)

在 vibecoding 之前过一遍,写代码时主动覆盖。

- [ ] 员工在领取窗口期内离职 → HR 状态同步后自动取消资格
- [ ] 员工从 A 楼调到 B 楼 → 已预约的不变,新预约用新楼宇
- [ ] 同一员工同一批次重复点击预约 → 幂等,返回已有 claim
- [ ] SKU 库存归零时还有人在选 → 实时校验,前端禁选并提示
- [ ] 核销时网络中断 → 分仓 App 本地缓存,联网后同步
- [ ] 批次结束后还有未领 → 24h 自动过期,生成"未领清单"给行政
- [ ] 食品类礼物有过敏需求 → SKU 必填过敏原标签,员工选择前可见
- [ ] HR 数据延迟 → 行政可手动添加/移除资格
- [ ] 临时工/外包是否享受 → 配置项,默认排除
- [ ] 时间窗跨节假日 → 时间配置要支持工作日/自然日切换

---

## 12. 技术建议 (Vibecoding 友好)

考虑你要做 vibecoding,推荐:

**前端**: Next.js 14 (App Router) + TypeScript + Tailwind + shadcn/ui  
**后端**: Next.js API Routes (一体化,vibecoding 最快)  
**数据库**: Supabase (Postgres + Auth + Row Level Security 一站式)  
**状态管理**: Zustand (轻) 或 React Query  
**二维码**: qrcode.react  
**Excel 导入**: SheetJS (xlsx)  
**通知 (Mock)**: 控制台日志 + UI Toast,真实集成放 V1.1  
**部署**: Vercel + Supabase

**Vibecoding 路线建议** (按这个顺序做,每步可独立跑通):

```
Step 1: 数据建模 + Supabase 建表 + 种子数据 (50 个假员工、5 个 SKU、3 个楼宇)
Step 2: 管理后台 - SKU + 库存 CRUD
Step 3: 管理后台 - 批次创建 (含简单规则引擎)
Step 4: 员工 H5 - 查看可领 + 选择 + 生成码
Step 5: 分仓端 - 扫码核销 (用 react-qr-reader)
Step 6: 数据看板 - 用 Recharts 出 3 个核心图表
Step 7: 异常流程 + 状态机收尾
```

---

## 13. 待澄清问题 (Open Questions)

开发前最好和真实行政同学/HR 对齐,这些问题影响设计选择:

1. **HR 数据源**:HR 系统能否提供实时 API,还是只能定时同步?如果定时,接受多长延迟?
2. **IM 通道**:确定为飞书 + 邮件双通道。飞书 OpenAPI 通道接入方式?邮件 SMTP / 模板?
3. **采购 → 入库链路**:行政如何完成入库登记?手动录入 vs 扫供应商发货单?
4. **预算审批**:是否需要在创建批次时关联预算审批流?
5. **领取地点偏好**:员工默认按所在楼宇,还是允许自由选(可能造成单楼宇爆仓)?
6. **历史数据**:过去的 Excel 数据是否需要迁入?
7. **审计要求**:是否需要详细操作日志(谁在何时改了什么)?
8. **多语言**:外籍员工占比?是否需要英文版?
9. **配送场景**:孕妇/出差员工真实占比,是否值得做?
10. **离职员工已预约**:政策上是允许领走还是必须退回?

---

## 14. 验收清单 (Definition of Done)

MVP 上线时这些必须都能通过:

- [ ] 行政可以在 10 分钟内创建一个完整批次(从零开始)
- [ ] 员工从收到通知到拿到二维码不超过 1 分钟、不超过 4 次点击
- [ ] 分仓核销单次操作 ≤ 3 秒
- [ ] 库存任何变更都有完整审计日志
- [ ] 数据看板秒级响应
- [ ] 系统在 200 人同时领取时不挂
- [ ] 一份英文/中文都看得懂的使用说明
- [ ] 一份给行政的 5 分钟培训视频

---

## 附录 A:Vibecoding 起手 Prompt 模板

你可以直接把下面这段贴给 AI 编程助手:

```
我要做一个企业内部的节日福利礼物发放系统。
请基于附件的 PRD 文档,用 Next.js 14 + TypeScript + Tailwind + 
shadcn/ui + Supabase 搭建项目骨架。

第一步,请帮我:
1. 初始化 Next.js 项目结构,App Router
2. 配置 Supabase 客户端 (使用环境变量占位)
3. 按 PRD 第 8 节的数据模型,生成 Supabase migration SQL
4. 生成种子数据脚本:50 个员工、5 个 SKU、3 个楼宇
5. 设置基础布局:管理后台 / 员工 H5 / 分仓端 三个路由组

不要一次性做完所有功能。完成 Step 1 后停下,等我确认后再做 Step 2。
```

---

*文档版本:v0.1 (开发前基线)*  
*下一次更新触发:与真实行政同学访谈后、MVP 上线后*
